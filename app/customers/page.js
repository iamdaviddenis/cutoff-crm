import { CustomersListPage } from "../../components/customers-list";

export const metadata = { title: "Customers — CutOff CRM" };

export default function CustomersPage() {
  return <CustomersListPage />;
}
