import { InteractionsListPage } from "../../components/interactions-list";

export const metadata = { title: "Interactions — CutOff CRM" };

export default function InteractionsPage() {
  return <InteractionsListPage />;
}
